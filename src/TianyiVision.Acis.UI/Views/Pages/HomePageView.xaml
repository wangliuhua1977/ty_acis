<UserControl x:Class="TianyiVision.Acis.UI.Views.Pages.HomePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:state="clr-namespace:TianyiVision.Acis.UI.States"
             Loaded="HomePageView_OnLoaded"
             SizeChanged="HomePageView_OnSizeChanged">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <Style x:Key="OverlayPanelStyle" TargetType="Border">
            <Setter Property="Padding" Value="14" />
            <Setter Property="CornerRadius" Value="18" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="{DynamicResource Theme.PanelBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.PanelBorderBrush}" />
            <Setter Property="Opacity" Value="0.94" />
        </Style>

        <Style x:Key="OverlayFaultButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryActionButtonStyle}">
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Theme.DangerSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource Theme.DangerBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RestoreOverlayButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryActionButtonStyle}">
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="MinWidth" Value="108" />
            <Setter Property="Opacity" Value="0.84" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tag.IsVisible, RelativeSource={RelativeSource Self}}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Theme.AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource Theme.AccentBrush}" />
                    <Setter Property="Opacity" Value="1" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="OverlayCloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Margin" Value="12,0,0,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.TextSecondaryBrush}" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Opacity" Value="0.68" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                Background="Transparent"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource Theme.PanelBackgroundBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimaryBrush}" />
                                <Setter Property="Opacity" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HomeMapPointButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="96" />
            <Setter Property="Height" Value="96" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Width="96" Height="96" RenderTransformOrigin="0.5,0.5">
                            <Grid.RenderTransform>
                                <ScaleTransform />
                            </Grid.RenderTransform>
                            <Ellipse x:Name="PulseRing" Width="74" Height="74" Fill="{DynamicResource Theme.InspectionActiveSoftBrush}" Opacity="0" />
                            <Ellipse x:Name="FaultRing" Width="80" Height="80" Fill="{DynamicResource Theme.DangerSoftBrush}" Opacity="0" />
                            <Border x:Name="CoreBorder" Width="52" Height="52" CornerRadius="26" Background="{DynamicResource Theme.AccentSoftBrush}" BorderBrush="{DynamicResource Theme.PanelBorderStrongBrush}" BorderThickness="1" VerticalAlignment="Top" HorizontalAlignment="Center">
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Margin="8" FontSize="10" FontWeight="SemiBold" Text="{Binding Name}" TextAlignment="Center" TextWrapping="Wrap" />
                            </Border>
                            <Border x:Name="SelectionTag" Width="16" Height="16" CornerRadius="8" Background="{DynamicResource Theme.AccentBrush}" BorderBrush="{DynamicResource Theme.TextPrimaryBrush}" BorderThickness="1" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0" Visibility="Collapsed" />
                            <TextBlock Margin="0,64,0,0" Width="94" HorizontalAlignment="Center" FontSize="11" Text="{Binding Name}" TextAlignment="Center" TextWrapping="Wrap" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding Kind}" Value="{x:Static state:HomeMapPointKind.Normal}">
                                <Setter TargetName="CoreBorder" Property="Background" Value="{DynamicResource Theme.SuccessSoftBrush}" />
                                <Setter TargetName="CoreBorder" Property="BorderBrush" Value="{DynamicResource Theme.SuccessBrush}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Kind}" Value="{x:Static state:HomeMapPointKind.Fault}">
                                <Setter TargetName="CoreBorder" Property="Background" Value="{DynamicResource Theme.DangerSoftBrush}" />
                                <Setter TargetName="CoreBorder" Property="BorderBrush" Value="{DynamicResource Theme.DangerBrush}" />
                                <Setter TargetName="FaultRing" Property="Opacity" Value="0.65" />
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                            <DoubleAnimation Storyboard.TargetName="FaultRing" Storyboard.TargetProperty="Opacity" From="0.25" To="0.95" Duration="0:0:0.75" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Kind}" Value="{x:Static state:HomeMapPointKind.Key}">
                                <Setter TargetName="CoreBorder" Property="Background" Value="{DynamicResource Theme.WarningSoftBrush}" />
                                <Setter TargetName="CoreBorder" Property="BorderBrush" Value="{DynamicResource Theme.WarningBrush}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Kind}" Value="{x:Static state:HomeMapPointKind.Inspecting}">
                                <Setter TargetName="CoreBorder" Property="Background" Value="{DynamicResource Theme.InspectionActiveSoftBrush}" />
                                <Setter TargetName="CoreBorder" Property="BorderBrush" Value="{DynamicResource Theme.InspectionActiveBrush}" />
                                <Setter TargetName="PulseRing" Property="Opacity" Value="0.75" />
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetName="PulseRing" Storyboard.TargetProperty="Opacity" From="0.25" To="0.8" Duration="0:0:0.8" AutoReverse="True" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter TargetName="SelectionTag" Property="Visibility" Value="Visible" />
                                <Setter TargetName="CoreBorder" Property="BorderThickness" Value="2" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="RootMapStage" ClipToBounds="True" Background="{DynamicResource Theme.WorkbenchBackgroundBrush}">
        <Canvas>
            <Line X1="20" Y1="90" X2="1260" Y2="90" Stroke="{DynamicResource Theme.MapGridOverlayBrush}" StrokeThickness="1" />
            <Line X1="20" Y1="220" X2="1260" Y2="220" Stroke="{DynamicResource Theme.MapGridOverlayBrush}" StrokeThickness="1" />
            <Line X1="20" Y1="350" X2="1260" Y2="350" Stroke="{DynamicResource Theme.MapGridOverlayBrush}" StrokeThickness="1" />
            <Line X1="20" Y1="480" X2="1260" Y2="480" Stroke="{DynamicResource Theme.MapGridOverlayBrush}" StrokeThickness="1" />
            <Line X1="20" Y1="610" X2="1260" Y2="610" Stroke="{DynamicResource Theme.MapGridOverlayBrush}" StrokeThickness="1" />
            <Line X1="110" Y1="20" X2="110" Y2="780" Stroke="{DynamicResource Theme.MapGridOverlayBrush}" StrokeThickness="1" />
            <Line X1="310" Y1="20" X2="310" Y2="780" Stroke="{DynamicResource Theme.MapGridOverlayBrush}" StrokeThickness="1" />
            <Line X1="510" Y1="20" X2="510" Y2="780" Stroke="{DynamicResource Theme.MapGridOverlayBrush}" StrokeThickness="1" />
            <Line X1="710" Y1="20" X2="710" Y2="780" Stroke="{DynamicResource Theme.MapGridOverlayBrush}" StrokeThickness="1" />
            <Line X1="910" Y1="20" X2="910" Y2="780" Stroke="{DynamicResource Theme.MapGridOverlayBrush}" StrokeThickness="1" />
            <Line X1="1110" Y1="20" X2="1110" Y2="780" Stroke="{DynamicResource Theme.MapGridOverlayBrush}" StrokeThickness="1" />
            <Path Stroke="{DynamicResource Theme.AccentSecondaryBrush}" StrokeThickness="3" Opacity="0.40" Data="M 60,540 C 240,320 430,400 610,230 S 930,150 1160,300 1210,460 1240,420" />
            <Path Stroke="{DynamicResource Theme.PanelBorderStrongBrush}" StrokeThickness="3" Opacity="0.32" Data="M 80,180 C 260,360 360,390 560,470 720,530 950,470 1180,210" />
            <Path Stroke="{DynamicResource Theme.AccentBrush}" StrokeThickness="4" Opacity="0.20" Data="M 180,70 C 310,90 490,110 660,120 870,132 1010,128 1200,90" />
        </Canvas>

        <ItemsControl ItemsSource="{Binding MapPoints}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}" />
                    <Setter Property="Canvas.Top" Value="{Binding Y}" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type state:HomeMapPointState}">
                    <Button Style="{StaticResource HomeMapPointButtonStyle}" Command="{Binding DataContext.SelectMapPointCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="{Binding Name}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,18,18,0" Panel.ZIndex="60" Style="{StaticResource OverlayPanelStyle}" Padding="12">
            <StackPanel>
                <WrapPanel>
                    <Button Style="{StaticResource RestoreOverlayButtonStyle}" Tag="{Binding TaskPanel}" Click="RestoreTaskPanel_OnClick">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Margin="0,0,8,0" Fill="{DynamicResource Theme.TextSecondaryBrush}" VerticalAlignment="Center" />
                            <TextBlock VerticalAlignment="Center" Text="{Binding TaskPanelTitle}" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource RestoreOverlayButtonStyle}" Tag="{Binding FaultPanel}" Click="RestoreFaultPanel_OnClick">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Margin="0,0,8,0" Fill="{DynamicResource Theme.TextSecondaryBrush}" VerticalAlignment="Center" />
                            <TextBlock VerticalAlignment="Center" Text="{Binding FaultPanelTitle}" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource RestoreOverlayButtonStyle}" Tag="{Binding PointPanel}" Click="RestorePointPanel_OnClick">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Margin="0,0,8,0" Fill="{DynamicResource Theme.TextSecondaryBrush}" VerticalAlignment="Center" />
                            <TextBlock VerticalAlignment="Center" Text="{Binding PointPanelTitle}" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource RestoreOverlayButtonStyle}" Tag="{Binding LegendPanel}" Click="RestoreLegendPanel_OnClick">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Margin="0,0,8,0" Fill="{DynamicResource Theme.TextSecondaryBrush}" VerticalAlignment="Center" />
                            <TextBlock VerticalAlignment="Center" Text="{Binding LegendPanelTitle}" />
                        </StackPanel>
                    </Button>
                </WrapPanel>
                <Button Margin="0,8,0,0" Style="{StaticResource RestoreOverlayButtonStyle}" Command="{Binding ResetOverlayLayoutCommand}" Content="{Binding ResetOverlayLayoutText}" />
            </StackPanel>
        </Border>

        <Canvas x:Name="OverlayCanvas" Background="Transparent" Panel.ZIndex="30">
            <Border x:Name="TaskPanelBorder" Width="300" Panel.ZIndex="30" Canvas.Left="0" Canvas.Top="0" DataContext="{Binding TaskPanel}" Style="{StaticResource OverlayPanelStyle}" Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <DockPanel LastChildFill="True">
                    <StackPanel DockPanel.Dock="Top">
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="Transparent" Cursor="SizeAll" MouseLeftButtonDown="OverlayHeader_OnMouseLeftButtonDown" MouseLeftButtonUp="OverlayHeader_OnMouseLeftButtonUp" MouseMove="OverlayHeader_OnMouseMove" Tag="{Binding}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="{Binding DataContext.TaskPanelTitle, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                    <TextBlock Margin="0,6,0,0" Style="{StaticResource PageDescriptionTextStyle}" Text="{Binding DataContext.TaskPanelDescription, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                </StackPanel>
                            </Border>
                            <Button Grid.Column="1" Style="{StaticResource OverlayCloseButtonStyle}" Command="{Binding DataContext.HideOverlayPanelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Content="×" />
                        </Grid>
                        <TextBlock Style="{StaticResource SectionLabelTextStyle}" Text="{Binding DataContext.CurrentGroupLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <TextBlock Margin="0,8,0,0" Style="{StaticResource FieldValueTextStyle}" Text="{Binding DataContext.CurrentGroupSummary, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <TextBlock Margin="0,14,0,0" Style="{StaticResource SectionLabelTextStyle}" Text="{Binding DataContext.ExecutionProgressLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <TextBlock Margin="0,8,0,0" Style="{StaticResource FieldValueTextStyle}" Text="{Binding DataContext.ExecutionProgress, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <ProgressBar Margin="0,10,0,0" Height="8" Maximum="12" Value="8" />
                        <TextBlock Margin="0,14,0,0" Style="{StaticResource SectionLabelTextStyle}" Text="{Binding DataContext.PendingReviewLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <TextBlock Margin="0,8,0,0" Style="{StaticResource PageDescriptionTextStyle}" Text="{Binding DataContext.PendingReviewSummary, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <TextBlock Margin="0,14,0,0" Style="{StaticResource SectionLabelTextStyle}" Text="{Binding DataContext.PendingDispatchLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <TextBlock Margin="0,8,0,0" Style="{StaticResource PageDescriptionTextStyle}" Text="{Binding DataContext.PendingDispatchSummary, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border x:Name="FaultPanelBorder" Width="320" Panel.ZIndex="30" Canvas.Left="0" Canvas.Top="0" DataContext="{Binding FaultPanel}" Style="{StaticResource OverlayPanelStyle}" Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="Transparent" Cursor="SizeAll" MouseLeftButtonDown="OverlayHeader_OnMouseLeftButtonDown" MouseLeftButtonUp="OverlayHeader_OnMouseLeftButtonUp" MouseMove="OverlayHeader_OnMouseMove" Tag="{Binding}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="{Binding DataContext.FaultPanelTitle, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                <TextBlock Margin="0,6,0,0" Style="{StaticResource PageDescriptionTextStyle}" Text="{Binding DataContext.FaultPanelDescription, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                            </StackPanel>
                        </Border>
                        <Button Grid.Column="1" Style="{StaticResource OverlayCloseButtonStyle}" Command="{Binding DataContext.HideOverlayPanelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Content="×" />
                    </Grid>
                    <ItemsControl ItemsSource="{Binding DataContext.RecentFaults, RelativeSource={RelativeSource AncestorType=UserControl}}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type state:HomeRecentFaultState}">
                                <Button Style="{StaticResource OverlayFaultButtonStyle}" Command="{Binding DataContext.SelectRecentFaultCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock FontWeight="SemiBold" Text="{Binding PointName}" />
                                            <TextBlock Margin="0,6,0,0" Style="{StaticResource PageDescriptionTextStyle}" Text="{Binding FaultType}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Style="{StaticResource SectionLabelTextStyle}" Text="{Binding DataContext.RecentFaultTimeLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                            <TextBlock Margin="0,6,0,0" Style="{StaticResource FieldValueTextStyle}" Text="{Binding LatestFaultTime}" />
                                        </StackPanel>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border x:Name="PointPanelBorder" Width="320" Panel.ZIndex="30" Canvas.Left="0" Canvas.Top="0" DataContext="{Binding PointPanel}" Style="{StaticResource OverlayPanelStyle}" Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="Transparent" Cursor="SizeAll" MouseLeftButtonDown="OverlayHeader_OnMouseLeftButtonDown" MouseLeftButtonUp="OverlayHeader_OnMouseLeftButtonUp" MouseMove="OverlayHeader_OnMouseMove" Tag="{Binding}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="{Binding DataContext.PointPanelTitle, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                <TextBlock Margin="0,6,0,0" Style="{StaticResource PageDescriptionTextStyle}" Text="{Binding DataContext.PointPanelDescription, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                            </StackPanel>
                        </Border>
                        <Button Grid.Column="1" Style="{StaticResource OverlayCloseButtonStyle}" Command="{Binding DataContext.HideOverlayPanelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Content="×" />
                    </Grid>
                    <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding DataContext.SelectedPointSummary.PointName, RelativeSource={RelativeSource AncestorType=UserControl}}" TextWrapping="Wrap" />
                    <TextBlock Margin="0,8,0,0" Style="{StaticResource PageDescriptionTextStyle}" Text="{Binding DataContext.SelectedPointSummary.UnitName, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                    <Grid Margin="0,14,0,0">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="94" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                        <TextBlock Style="{StaticResource FieldLabelTextStyle}" Text="{Binding DataContext.SelectedPointStatusLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <TextBlock Grid.Column="1" Style="{StaticResource FieldValueTextStyle}" Text="{Binding DataContext.SelectedPointSummary.StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <TextBlock Grid.Row="1" Margin="0,12,0,0" Style="{StaticResource FieldLabelTextStyle}" Text="{Binding DataContext.SelectedPointFaultTypeLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,12,0,0" Style="{StaticResource FieldValueTextStyle}" Text="{Binding DataContext.SelectedPointSummary.FaultType, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <TextBlock Grid.Row="2" Margin="0,12,0,0" Style="{StaticResource FieldLabelTextStyle}" Text="{Binding DataContext.SelectedPointSummaryLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,12,0,0" Style="{StaticResource PageDescriptionTextStyle}" Text="{Binding DataContext.SelectedPointSummary.Summary, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                    </Grid>
                    <Border Margin="0,14,0,0" Style="{StaticResource MutedPanelStyle}" Padding="12">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionLabelTextStyle}" Text="{Binding DataContext.SelectedPointActionLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                            <TextBlock Margin="0,8,0,0" Style="{StaticResource PageDescriptionTextStyle}" Text="{Binding DataContext.SelectedPointSummary.ActionHint, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                            <Button Margin="0,12,0,0"
                                    Style="{StaticResource SecondaryActionButtonStyle}"
                                    Command="{Binding DataContext.OpenDispatchWorkspaceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Content="{Binding DataContext.OpenDispatchWorkspaceText, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Border x:Name="LegendPanelBorder" Width="520" Panel.ZIndex="30" Canvas.Left="0" Canvas.Top="0" DataContext="{Binding LegendPanel}" Style="{StaticResource OverlayPanelStyle}" Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="Transparent" Cursor="SizeAll" MouseLeftButtonDown="OverlayHeader_OnMouseLeftButtonDown" MouseLeftButtonUp="OverlayHeader_OnMouseLeftButtonUp" MouseMove="OverlayHeader_OnMouseMove" Tag="{Binding}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="{Binding DataContext.LegendPanelTitle, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                <TextBlock Margin="0,6,0,0" Style="{StaticResource PageDescriptionTextStyle}" Text="{Binding DataContext.MapSelectionHint, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                            </StackPanel>
                        </Border>
                        <Button Grid.Column="1" Style="{StaticResource OverlayCloseButtonStyle}" Command="{Binding DataContext.HideOverlayPanelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Content="×" />
                    </Grid>
                    <WrapPanel>
                        <Border Style="{StaticResource MutedPanelStyle}" Padding="12,8" Margin="0,0,10,0"><TextBlock Style="{StaticResource FieldValueTextStyle}" Text="{Binding DataContext.LegendFaultText, RelativeSource={RelativeSource AncestorType=UserControl}}" /></Border>
                        <Border Style="{StaticResource MutedPanelStyle}" Padding="12,8" Margin="0,0,10,0"><TextBlock Style="{StaticResource FieldValueTextStyle}" Text="{Binding DataContext.LegendNormalText, RelativeSource={RelativeSource AncestorType=UserControl}}" /></Border>
                        <Border Style="{StaticResource MutedPanelStyle}" Padding="12,8" Margin="0,0,10,0"><TextBlock Style="{StaticResource FieldValueTextStyle}" Text="{Binding DataContext.LegendKeyText, RelativeSource={RelativeSource AncestorType=UserControl}}" /></Border>
                        <Border Style="{StaticResource MutedPanelStyle}" Padding="12,8"><TextBlock Style="{StaticResource FieldValueTextStyle}" Text="{Binding DataContext.LegendInspectingText, RelativeSource={RelativeSource AncestorType=UserControl}}" /></Border>
                    </WrapPanel>
                </StackPanel>
            </Border>
        </Canvas>
    </Grid>
</UserControl>
