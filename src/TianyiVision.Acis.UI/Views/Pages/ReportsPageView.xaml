<UserControl x:Class="TianyiVision.Acis.UI.Views.Pages.ReportsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,20">
            <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="{Binding Title}" />
            <TextBlock Margin="0,10,0,0" Style="{StaticResource PageDescriptionTextStyle}" Text="{Binding Description}" />
        </StackPanel>

        <ContentControl Grid.Row="1"
                        Margin="0,0,0,12"
                        Content="{Binding FilterPanel}"
                        ContentTemplate="{StaticResource PanelPlaceholderTemplate}" />

        <ItemsControl Grid.Row="2"
                      Margin="0,0,0,12"
                      ItemsSource="{Binding Metrics}"
                      ItemTemplate="{StaticResource MetricCardTemplate}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ItemsControl ItemsSource="{Binding ChartPanels}" ItemTemplate="{StaticResource PanelPlaceholderTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <ContentControl Grid.Row="1"
                            Margin="0,12,0,0"
                            Content="{Binding TablePanel}"
                            ContentTemplate="{StaticResource PanelPlaceholderTemplate}" />
        </Grid>
    </Grid>
</UserControl>
